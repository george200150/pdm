import {NECoords} from "./NECoords";

export interface ItemProps {
    id?: string;
    name: string;
    // bloomingDate?: Date;
    // hasFlowers: boolean;
    // location: NECoords;
    // photo: Blob;
}
